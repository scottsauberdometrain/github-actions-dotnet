name: Run every hour

on:
  schedule:
    - cron: "0 * * * *"

jobs:
  cron:
    name: Run every 5 minutes
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        run: echo "Hello World"
